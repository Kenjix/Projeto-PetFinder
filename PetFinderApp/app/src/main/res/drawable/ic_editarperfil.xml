<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Adicione o fundo redondo -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#FFFFFF" /> <!-- Define a cor de fundo -->
        </shape>
    </item>

    <!-- Adicione o ícone -->
    <item>
        <inset
            android:drawable="@drawable/editar"
            android:inset="1dp" />
    </item>
</layer-list>
